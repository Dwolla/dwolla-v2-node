/**
 * Represents the current verification status of a beneficial owner.
 */
export const enum VerificationStatus {
    DOCUMENT = "document",
    INCOMPLETE = "incomplete",
    VERIFIED = "verified"
}
