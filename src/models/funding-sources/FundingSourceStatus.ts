/**
 * Defines the status of a {@link FundingSource} — either `unverified` or `verified`.
 */
export const enum FundingSourceStatus {
    /**
     * Unverified
     */
    UNVERIFIED = "unverified",

    /**
     * Verified
     */
    VERIFIED = "verified"
}
